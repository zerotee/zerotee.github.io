PATH := ../node_modules/.bin:$(PATH)

# Settings
publicDir = ..
assetsDir = $(publicDir)/assets
jsNom     = $(publicDir)/node_modules

# Utils
space :=
space +=
comma := ,

include *.mk

print-%:
	@echo "$($*)"

.PHONY: all clean
